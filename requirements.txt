# Requirements for the HoloDepth project.
#
# --- IMPORTANT ---
# This file does NOT install the GPU version of PyTorch.
# For GPU support, you MUST install PyTorch and Torchvision manually FIRST.
# Follow the instructions for your OS and CUDA version on the official PyTorch website:
# https://pytorch.org/
#
# Once PyTorch is installed, you can install the remaining packages with:
# pip install -r requirements.txt
# -----------------

numpy>=1.21
Pillow>=9.0
tqdm>=4.60
